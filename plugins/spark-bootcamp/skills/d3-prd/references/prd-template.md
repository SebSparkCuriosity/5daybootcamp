# Build Brief (PRD): <product name>

*Written on Day 3. Built from `story-map.md` and `moscow.md`. This is the one document the build follows.*

## The outcome, as a number

By end of Day 3, a real prospect can **<do this specific thing>**.
We prove it with one metric: **<number> <what> by <deadline>**.

Example: "3 real prospects complete a trust-review request and receive a booking confirmation by Friday 17:00."

## Requirements

Every Must from the MoSCoW cut, one requirement each, each with an acceptance test.

### R1: <the system must do this specific thing>
- **Traces to:** <story-map step>
- **Acceptance:** <the one observable check that proves it works, with a number where one applies>

### R2: <...>
- **Traces to:** <...>
- **Acceptance:** <...>

### R3: <...>
- **Traces to:** <...>
- **Acceptance:** <...>

*(Aim for 3 to 5 requirements. More than 7 and you have not cut hard enough.)*

## Non-goals (not in scope this week)

Everything you are deliberately not building. Pulled from Won't, plus every Should you are parking.

- Not building: <thing>
- Not building: <thing>
- Later, not now: <Should item>

## Success metric

<Restate the single number and its deadline. This is what Friday is judged against.>

---

## Notes by path

**Software:** requirements describe user-visible behaviour, not implementation. Name the one table behind the slice in the functional spec, not here.

**Hardware:** two blocks of requirements, prototype and intent page. The metric is signed-up intent (deposit or card hold), not emails.

**Services:** requirements describe the productised package and the one sample deliverable. The metric is booked intakes.
